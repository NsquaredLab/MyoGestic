"""Reference / baseline implementations that aren't part of the core framework.

The framework deliberately stays out of feature engineering, model choice,
and other domain decisions. ``myogestic.contrib`` is where common
conveniences live so examples don't re-implement them every time —
treat anything here as a *starting point* you'll usually swap or extend.
"""
