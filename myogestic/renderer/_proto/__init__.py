"""Generated gRPC stubs for the renderer control plane.

The contract is defined in ``renderer_control.proto`` (vendored byte-for-byte from the
Virtual-Hand-Interface repo, which is where it is authored). ``renderer_control_pb2.py``
and ``renderer_control_pb2_grpc.py`` are generated — regenerate with
``uv run --extra grpc python tools/gen_proto.py``.
"""
