"""Generated gRPC stubs for the remote control plane.

The contract is defined in ``remote_control.proto`` (vendored byte-for-byte from the
Virtual-Hand-Interface repo, which is where it is authored). ``remote_control_pb2.py``
and ``remote_control_pb2_grpc.py`` are generated — regenerate with
``uv run --extra grpc python tools/gen_proto.py``.
"""
