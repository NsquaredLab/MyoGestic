# MyoGestic — public API parameter inventory

_Auto-generated by `tools/gen_param_inventory.py` for naming cross-reference._
_Names may repeat across a subpackage and the top-level facade (re-exports)._

## `myogestic`

- **class `App`**(`name, theme, docking, ui_scale`)
  - `bridges` — `bridges`
  - `popout` — `title, gui_fn, default_open, can_be_closed, remember_is_visible`
  - `run` — `mode, window_size, fullscreen`
  - `start_recording` — `base_path`
  - `stop_recording` — ``
  - `streams` — `streams`
  - `ui` — `fn`
- **class `AppState`**(`values`)
- **class `Context`**(`streams, bridges, state, session, class_names, current_label, status_message, logs`)
  - `log` — `message, max_lines`
- **class `EdgeTrigger`**(`callback, stable_ticks`)
  - `fire_if_changed` — `value`
  - `last` _(property)_
  - `rebase` — `value`
- **class `Fr`**(`value`)
- **class `Grid`**(`rows, cols, row_height, col_width`)
  - `end_frame` — ``
- **class `Pipeline`**(`app, predict_hz`)
  - `extract` — `fn`
  - `predict` — `fn`
  - `start_predicting` — ``
  - `start_training` — ``
  - `stop_predicting` — ``
  - `train` — `fn`
- **class `Px`**(`value`)
- **class `Source`**(`args, kwargs`)
  - `connect` — ``
  - `disconnect` — ``
  - `read` — ``
- **class `Stream`**(`name, source, window_ms, buffer_ms`)
  - `attach_session` — `session`
  - `detach_session` — ``
  - `get_display` — `n_pixels`
  - `get_raw_snapshot` — ``
  - `get_window` — ``
  - `last_timestamp` — ``
  - `reconnect` — `target`
  - `start` — ``
  - `stop` — ``
- **class `StreamInfo`**(`n_channels, fs, dtype, channel_names`)
- **class `TrainingData`**(`paths, class_names, classes`)
  - `is_empty` _(property)_

## `myogestic.sources`

- **class `LSLSource`**(`stream_name, dtype`)
  - `connect` — ``
  - `disconnect` — ``
  - `discover` — ``
  - `read` — ``
  - `reconnect` — `target`
- **class `ReplaySource`**(`session_path, stream_name, speed`)
  - `connect` — ``
  - `disconnect` — ``
  - `read` — ``

## `myogestic.outputs`

- **class `GaussianFilter`**(`window, sigma`)
  - `reset` — ``
- **class `IdentityFilter`**(``)
  - `reset` — ``
- **class `LSLOutlet`**(`name, n_channels, hz`)
- **class `OneEuroFilter`**(`hz, min_cutoff_hz, beta, derivative_cutoff_hz`)
  - `reset` — ``
- **class `Output`**(`hz`)
  - `push` — `data`
  - `stop` — ``
- **class `UDPOutput`**(`host, port, hz`)
  - `stop` — ``
- **class `VectorFilter`**(`args, kwargs`)
  - `reset` — ``
- func `make_filter`(`name, hz, kwargs`)

## `myogestic.ml`

- **class `PipelineState`**(`values`)
- func `load_pickle`(`path`)
- func `save_pickle`(`model, path`)

## `myogestic.ml.widgets`

- func `load_model_button`(`pipeline, path, size`)
- func `panel_header`(`title, icon`)
- func `pipeline_panel`(`pipeline, log_height, widget_id`)
- func `predict_button`(`pipeline, size`)
- func `render_log`(`widget_id, lines, height, autoscroll`)
- func `render_log_buttons`(`widget_id, autoscroll, popped_out`)
- func `render_log_popout`(`widget_id, lines, title, autoscroll`)
- func `save_model_button`(`pipeline, path, size`)
- func `train_button`(`pipeline, size`)
- func `training_log`(`pipeline, height, widget_id`)

## `myogestic.recipes.features`

- func `mav`(`emg`)
- func `rms`(`emg`)
- func `var`(`emg`)
- func `wl`(`emg`)
- func `zc`(`emg`)

## `myogestic.recipes.estimators`

- func `catboost_classifier`(`kwargs`)
- func `catboost_regressor`(`kwargs`)
- func `constant_classifier`(`class_idx`)
- func `mean_regressor`(``)
- func `sklearn_classifier`(`kwargs`)
- func `sklearn_extra_trees_classifier`(`kwargs`)
- func `sklearn_extra_trees_regressor`(`kwargs`)
- func `sklearn_logistic_classifier`(`kwargs`)
- func `sklearn_regressor`(`kwargs`)

## `myogestic.session`

- **class `LabelEvent`**(`timestamp, class_index`)
- **class `Recording`**(`class_index, class_name, data, timestamps`)
- **class `Session`**(`base_path`)
  - `add_label` — `class_index, timestamp`
  - `append` — `name, data, timestamps`
  - `get_continuous` — `stream_name`
  - `get_trials` — `stream_name, pre, post, class_names`
  - `init_stream` — `name, info`
  - `pack_to_zip` — ``
  - `save_meta` — `app_name, class_names`
  - `stream_info` — `stream_name`
- func `iter_aligned_windows`(`paths, primary_stream, aligned_streams, window_ms, hop_ms, align_window_samples`)
- func `iter_labeled_windows`(`paths, stream_name, window_ms, hop_ms, classes`)
- func `open_session_store`(`path`)

## `myogestic.vhi.interfaces`

- **class `InterfaceSpec`**(`name, process, output_stream, n_output_channels, output_hz, control_stream, n_control_channels, control_pose_stream, n_control_pose_channels, control_pose_hz, grpc_host, grpc_port, install_root`)
  - `control_client` — ``
  - `control_outlet` — ``
  - `launcher` — ``
  - `outlet` — ``
- func `virtual_hand`(`godot_bin, vhi_path, grpc_host, grpc_port, mode`)

## `myogestic.widgets`

- **class `FeatureSelector`**(`features, default`)
  - `active_names` _(property)_
  - `is_active` — `name`
  - `n_active` _(property)_
  - `set_active` — `name, on`
  - `ui` — ``
- **class `FilterControl`**(`hz, default`)
  - `filter` _(property)_
  - `name` _(property)_
  - `reset` — ``
  - `ui` — `label`
- **class `TemplateInspectorRow`**(`key, label, accepted, info_text, energy`)
- **class `VhiMovementPanel`**(`client, on_movement, refresh_min_interval_s, label`)
  - `ui` — ``
- **class `VhiStateCache`**(`movements, current_movement, current_state, mode, connected, refreshing, message, last_attempt_s, lock`)
  - `snapshot` — ``
- **class `VhiStateSnapshot`**(`movements, current_movement, current_state, mode, connected, message`)
- func `app_logo`(`max_size, padding`)
- func `heatmap`(`label, data, size, label_fmt`)
- func `line_plot`(`label, data, channel_names, size`)
- func `log_panel`(`ctx, height, title, show_header`)
- func `popout_panel`(`title, gui_fn, default_open, can_be_closed, remember_is_visible`)
- func `prediction_label`(`pipeline, class_names, key, proba_key, label, show_probability, font_scale`)
- func `process_launcher`(`processes, label, log_height`)
- func `raw_signal_viewer`(`ctx, stream_name, size, channel_height`)
- func `recording_controls`(`ctx, class_names, on_record, on_stop, on_gesture`)
- func `request_vhi_state_refresh`(`client, cache, force, min_interval_s, disconnected_interval_s, probe_timeout_s`)
- func `scatter2d`(`label, points, labels, class_names, size, marker_size`)
- func `scatter3d`(`label, points, labels, class_names, size, axis_names`)
- func `session_manager`(`base_path, label, class_names`)
- func `signal_viewer`(`ctx, stream_name, size, n_pixels, channel_height, show_diagnostics, selectable, scale_mode, y_range, show_markers, window_s`)
- func `stream_panel`(`ctx, selectable, show_header`)
- func `template_inspector`(`uid, rows, title, height, label_colors`)
- func `trial_preview`(`uid, data, fs, data_layout, title, size, channel_names, band, band_color, gain, display_filter, scale_mode, y_range, window`)
- func `vhi_movement_palette`(`movements, on_movement, on_refresh, current_movement, connected, status, label`)

## `myogestic.tools.emg_generator`

- func `control_outlet`(`name`)
- func `main`(`name, channels, fs, chunk, classes, control, multi_dof`)
